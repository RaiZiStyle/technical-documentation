# Command line debugging with GDB

`break` : define a break point (can be function)
`run` : run the program
`next` : step over (`n`)
`tui enable` : enable text user interface (graphic)
`p`: print variable
`display` : display variable after each step





## Frontend debugging : 
gf2 : https://github.com/nakst/gf
